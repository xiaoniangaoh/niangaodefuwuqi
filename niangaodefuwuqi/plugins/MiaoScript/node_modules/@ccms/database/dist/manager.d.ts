import { database } from '@ccms/api';
import { DataBase } from './database';
export declare class DataBaseManager extends database.DataBaseManager {
    private mainDatabase;
    private databases;
    private Server;
    private webManager;
    constructor();
    startWebManager(...args: string[]): void;
    stopWebManager(): void;
    shutdown(): void;
    /**
     * 设置主数据库
     * @param mainDatabase 主数据库
     */
    setMainDatabase(mainDatabase: DataBase): void;
    /**
     * 获得主数据库
     * Get MainDatabase
     */
    getMainDatabase(): DataBase;
    /**
     * 创建数据库
     * Create A Database Instance
     * @param name 数据库名称 用于代码 database Name use at code
     * @param config 数据库配置
     */
    createDatabase(name: string, config: database.DataBaseConfig): any;
    removeDatabase(name: string): boolean;
    getDatabase(name: string): DataBase;
    disable(): void;
}
//# sourceMappingURL=manager.d.ts.map