import { ColumnMetadata } from "../metadata/column-metadata";
import { QueryBuilder } from "./query-builder";
export declare abstract class BaseQueryBuilder implements QueryBuilder {
    createColumn(name: string, type: ColumnMetadata): string;
    normalizeType(type: string): string;
    getAutoIncrement(): string;
}
//# sourceMappingURL=base-builder.d.ts.map