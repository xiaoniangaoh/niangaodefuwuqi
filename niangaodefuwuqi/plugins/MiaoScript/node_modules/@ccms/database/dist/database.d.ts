/// <reference types="@javatypes/jdk" />
import { database } from '@ccms/api';
import { QueryBuilder } from './query-builder/query-builder';
/**
 * 数据库封装类
 */
export declare class DataBase extends database.DataBase {
    private dbConfig;
    private queryBuilder;
    private dataSource;
    private jdbcTemplate;
    private HikariDataSource;
    private HikariConfig;
    private JdbcTemplate;
    constructor(dbConfig: database.DataBaseConfig);
    getQueryBuilder(): QueryBuilder;
    getDataSource(): javax.sql.DataSource;
    /**
     * 执行SQL查询
     * @param sql SQL语句
     * @param args 参数
     */
    query<T>(sql: string, ...args: any[]): Array<T>;
    /**
     * 执行SQL更新
     * @param sql SQL语句
     * @param args 参数
     */
    update(sql: string, ...args: any[]): number;
    /**
     * 执行SQL语句
     * @param sql SQL语句
     */
    execute(sql: string): void;
    close(): void;
    private createQueryBuilder;
    private createDataSource;
    private debug;
    private debugSQL;
}
//# sourceMappingURL=database.d.ts.map