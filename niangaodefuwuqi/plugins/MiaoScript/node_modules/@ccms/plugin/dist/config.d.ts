import { plugin } from '@ccms/api';
import { interfaces } from './interfaces';
import { PluginConfigLoader } from './config/interfaces';
import './config/loader';
export declare class PluginConfigManager {
    private container;
    private configLoaderMap;
    constructor();
    initialize(): void;
    getConfigLoader(format: string): PluginConfigLoader;
    loadConfig(plugin: plugin.Plugin): void;
    saveConfig(plugin: plugin.Plugin): void;
    createConfig(plugin: plugin.Plugin, metadata: interfaces.ConfigMetadata, value: any): any;
    private defienConfigProp;
    private loadConfig0;
    private setDefaultValue;
    private saveConfig0;
}
//# sourceMappingURL=config.d.ts.map