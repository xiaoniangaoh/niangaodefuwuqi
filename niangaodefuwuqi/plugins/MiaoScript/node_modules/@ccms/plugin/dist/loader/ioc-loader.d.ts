import { plugin } from "@ccms/api";
export declare class IocLoader implements plugin.PluginLoader {
    type: string;
    private container;
    private serverChecker;
    private pluginMetadataMap;
    constructor();
    require(loadMetadata: plugin.PluginLoadMetadata): plugin.PluginLoadMetadata;
    build(metadata: plugin.PluginMetadata): plugin.Plugin;
    load(plugin: plugin.Plugin): void;
    enable(plugin: plugin.Plugin): void;
    disable(plugin: plugin.Plugin): void;
    reload(plugin: plugin.Plugin): void;
    private bindPlugin;
    private stage;
}
//# sourceMappingURL=ioc-loader.d.ts.map