/// <reference types="@javatypes/jdk" />
import { plugin } from './index';
export declare namespace task {
    abstract class TaskManager {
        protected taskId: java.util.concurrent.atomic.AtomicInteger;
        protected cacheTasks: Map<number, Task>;
        protected pluginCacheTasks: Map<string, Map<number, Task>>;
        constructor();
        protected pluginCreate(plugin: plugin.Plugin, task: task.Task): Task;
        protected pluginDisable(plugin: plugin.Plugin): void;
        /**
         * 获得自增的任务ID
         */
        protected nextId(): number;
        /**
         * 创建任务
         * @param func 任务内容
         */
        create(func: Function, plugin?: plugin.Plugin): task.Task;
        cancel(taskId: number): void;
        /**
         * 在主线程执行代码
         * @param func 执行内容
         */
        abstract callSyncMethod(func: Function): any;
        /**
         * 关闭任务管理器
         */
        disable(plugin?: plugin.Plugin): void;
        protected abstract create0(owner: plugin.Plugin, func: Function, id: number): task.Task;
        protected abstract disable0(): any;
    }
    /**
     * 任务抽象
     */
    abstract class Task implements Cancelable {
        protected func: Function;
        protected isAsync: boolean;
        protected laterTime: number;
        protected interval: number;
        protected owner: plugin.Plugin;
        protected taskId: number;
        protected innerTask: any;
        constructor(owner: plugin.Plugin, func: Function, id: number);
        getOwner(): plugin.Plugin;
        getTaskId(): number;
        /**
         * 设置任务异步执行
         * @param isAsync 是否异步
         */
        async(isAsync?: boolean): task.Task;
        /**
         * 设置任务延时执行
         * @param tick 延时 Tick
         */
        later(tick: number): task.Task;
        /**
         * 设置任务循环执行
         * @param tick 循环时间 Tick
         */
        timer(tick: number): task.Task;
        /**
         * 取消任务
         */
        cancel(): boolean;
        protected run(...args: any[]): void;
        /**
         * 提交任务
         * @param args 任务参数
         */
        submit(...args: any[]): task.Task;
        /**
         * 提交任务
         * @param args 任务参数
         */
        protected abstract submit0(...args: any[]): any;
        /**
         * 取消任务
         */
        protected cancel0(): boolean;
    }
    /**
     * 返可取消的对象
     */
    interface Cancelable {
        cancel(): boolean;
    }
}
//# sourceMappingURL=task.d.ts.map