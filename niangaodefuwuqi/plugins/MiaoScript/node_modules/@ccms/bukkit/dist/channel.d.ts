import { channel } from '@ccms/api';
export declare class BukkitChannel extends channel.Channel {
    private pluginInstance;
    private cacheChannel;
    /**
     * 给玩家发送通道消息
     * @param player 接受消息的玩家
     * @param channel 通道
     * @param data 数据
     */
    send(player: any, channel: string, data: number[]): void;
    /**
     * 注册通道监听器
     * @param channel 通道
     * @param listener 监听器
     */
    register(channel: string, listener: channel.ChannelListener): any;
    unregister(channel: string, listener: any): void;
}
//# sourceMappingURL=channel.d.ts.map