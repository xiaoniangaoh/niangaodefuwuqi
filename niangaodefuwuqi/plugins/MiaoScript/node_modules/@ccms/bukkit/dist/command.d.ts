import '@ccms/nashorn';
import { command } from '@ccms/api';
export declare class BukkitCommand extends command.Command {
    private pluginInstance;
    private commandMap;
    private knownCommands;
    init(): void;
    create(plugin: any, command: string): any;
    remove(plugin: any, command: string): void;
    tabComplete(sender: any, input: string, index?: number): string[];
    onCommand(plugin: any, command: any, executor: Function): void;
    onTabComplete(plugin: any, command: any, tabCompleter: Function): void;
}
//# sourceMappingURL=command.d.ts.map